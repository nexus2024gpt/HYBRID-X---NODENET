# HYBRID X — NODENET

## 🚀 Overview
Hybrid X-NodeNet MVP v1.2 demonstrates a minimal architecture for resonance-based processing, trust calibration, and evolutionary node mutation.

## 📂 Structure
- `mvp/` — core simulation modules
- `app.py` — FastAPI backend
- `web/index.html` — frontend demo
- `requirements.txt` — dependencies

## ▶️ Run Locally
```bash
pip install -r requirements.txt
python mvp/mvp.py

